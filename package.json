{
  "name": "bad_game",
  "engines": {
    "node": ">=21.7.0",
    "npm": ">=10.5.0",
    "pnpm": "use npm",
    "yarn": "use npm"
  },
  "scripts": {
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
